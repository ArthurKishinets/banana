'use strict';

/**
 * @ngdoc service
 * @name bananaApp.wordsToTrain
 * @description
 * # wordsToTrain
 * Service in the bananaApp.
 */
angular.module('bananaApp')
  .factory('wordsToTrain',  function () {
    
  	/*var words = JSON.parse(localStorage.words);

	for(var i = 0; i < words.length; i++) {
		
		if ( words[i].String(training) === false ) {

			wordsToTrain.push($scope.words[i]);

		}

	}*/

	return  3;/*{

		'count': wordsToTrain.length,
		'words': wordsToTrain

	}*/


  });
